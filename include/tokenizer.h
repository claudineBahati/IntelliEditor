#ifndef TOKENIZER_H
#define TOKENIZER_H

#include "editor_core.h"

/**
 * Analyse le texte de l'éditeur et applique les styles via le formatter.
 * Version simplifiée pour le langage C.
 */
void tokenizer_run_c(EditorContext* ctx);

#endif // TOKENIZER_H
